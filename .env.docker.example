# PostgreSQL (Docker Compose) — copy to .env at repo root if you want overrides
POSTGRES_USER=evotec
POSTGRES_PASSWORD=evotec_secret
POSTGRES_DB=evotec
POSTGRES_PORT=5432

# Connection string for apps that use Postgres
DATABASE_URL=postgresql://evotec:evotec_secret@localhost:5432/evotec
